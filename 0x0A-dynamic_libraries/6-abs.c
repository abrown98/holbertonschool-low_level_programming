#include "main.h"
/**
  * _abs - takes the absolute value of a number
  * @n: absolute value of an integer
  * Return: Always 0 (Success)
  */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * (-1);
	}
return (n);
}
